import os
import sys

directory = "pyscripts"

path = os.getcwd()

location = os.path.join(path, directory)

created_dir = os.mkdir(location)

print(f"The {directory} is available at {location}")

import os
import sys

directory = "pyscripts"

path = os.getcwd()

location = os.path.join(path, directory)

created_dir = os.mkdir(location)

print(f"The {directory} is available at {location}")